node_modules/
.env
.env.*
secrets.env
registry.json
# Separate, unrelated project (own .git) that happens to sit in this folder --
# not part of era-dash-os, gitignored so deploy guards stop tripping on it.
ezee-bridge/
